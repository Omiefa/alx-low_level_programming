# include "main.h"
# include <string.h>

/**
 *_strcmp- function that compares two strings
 *@s1: input string
 *@s2: input string
 *Return: always 0
 */
int _strcmp(char *s1, char *s2)
{
	int m = strcmp(s1, s2);


	return (m);
}
